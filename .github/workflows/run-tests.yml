name: Run tests

on: pull_request

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
    - name: 'Check out code'
      uses: actions/checkout@v4

    - name: 'Run go test'
      run: 'go test ./...'
